# Keep installs consistent across Windows dev and Lightsail/Docker.
#
# - `engine-strict=true` fails installs if Node differs from package.json engines.
# - Removing pnpm-only flags avoids npm warnings and confusion.
engine-strict=true

